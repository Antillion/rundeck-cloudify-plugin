imports:
  - ../rundeck-cloudify-plugin.types.yaml

plugins:
  antillion:
    executor: central_deployment_agent
    source: ../../ # double ../ because Cfy local expects it in examples/plugins/the-plugin-dir-here

